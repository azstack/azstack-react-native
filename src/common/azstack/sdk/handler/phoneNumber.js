class PhoneNumber {
    constructor(options) { };

    getFromPhoneNumbers(options) {
        return new Promise((resolve, reject) => {
            resolve([]);
        });
    };
};

export default PhoneNumber;